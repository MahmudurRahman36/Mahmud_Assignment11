apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
  namespace: default
  labels:
    app: nginx
    part: assignment11
data:
  APP_MODE: "dev"
  APP_ENV: "development"
  APP_VERSION: "1.0"
